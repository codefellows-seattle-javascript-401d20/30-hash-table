'use strict';

const BTS = require('../model/BTS.js');

describe('#BinarySearchTree', () => {
  test('should have a value with next and previous properties equal to null', () => {
    
  });

  test('If this.value is less than root.value, this === root.previous', () => {

  });

  test('If this.value is greater than root.value, this === root.next', () => {

  });
});
